#!/usr/bin/env bash

function quoted() {
  printf '%s' "'${1//\'/\'\\\'\'}'"
}

[[ $# -eq 0 ]] && exit 0

printf "%s" "$(quoted "$1")"
shift

for arg in "$@"; do
  printf " %s" "$(quoted "$arg")"
done
