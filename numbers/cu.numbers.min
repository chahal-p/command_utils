#!/usr/bin/env bash

source cu.errors_lib.bash

m=$(getconf INT_MAX)
for x in "$@"
do
  cu.numbers.is_number $x || { cu.errors.echo "Input should be only numbers"; exit 1; }
  [ $x -lt $m ] && m=$x
done
echo -n "$m"