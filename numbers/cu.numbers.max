#!/usr/bin/env bash

source cu.errors_lib.bash

m=$(getconf INT_MIN)
for x in "$@"
do
  cu.numbers.is_number $x || cu.errors.die $CU_ERROR_INVALID_ARGUMENT "Input should be only numbers"
  [ $x -gt $m ] && m=$x
done
echo -n "$m"