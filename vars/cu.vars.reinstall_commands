#!/usr/bin/env bash

source _cu.vars.configs || exit 1

user="$(whoami)"
namespace="${NAMESPACE_PREFIX}_${user}"

_cu.persistent_kv list "${namespace}" 2>/dev/null | while read -r var; do
  cu.vars.create --var "${var}" || exit
done
